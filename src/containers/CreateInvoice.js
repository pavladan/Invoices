import React from 'react'
import {connect} from 'react-redux'

const CreateInvoice = (state)=>{
  return(
    <div className='CreateInvoice'>
      
    </div>
  )
}
export default connect(state=>({state}))(CreateInvoice)